node_modules/
dist/
.tsbuild/
*.log
.env
.DS_Store

# Frontend mock has its own git repo — not part of the backend repo
telegram-ui-clone/

# Vite build output served by nginx
client-build/
docker-compose.verify.yml
nginx/Dockerfile.verify
.playwright-mcp/
slice3-real-chat-verified.png

-- +goose Up
-- Место языка в выдаче `langpack.getLanguages`.
--
-- Порядок списка задаёт СЕРВЕР, потому что клиент его не задаёт: языки
-- рисуются перебором выдачи без всякой сортировки (tweb
-- `sidebarLeft/tabs/language.tsx:115`). Без этой колонки выдача шла по
-- английскому имени, то есть «English, French, German, Russian, Spanish,
-- Ukrainian», — а на экране настроек русский стоит вторым, и замена местного
-- списка серверной выдачей (задача 5) переставила бы его на четвёртое место.
-- Регресс, устроенный порядком строк в SQL.
--
-- Отдельной миграцией, а не правкой 0128: та уже накатана на стендах, а goose к
-- применённой версии не возвращается — правка её файла разъехалась бы со схемой
-- стенда молча.
--
-- Позицию проставляет сид из порядка списка языков в источнике
-- (`internal/langsource`, languageOrder) — там же, где лежат их паспорта.
-- DEFAULT 0 у существующих строк безвреден: сид перепишет его на первом же
-- старте, а до старта порядок остаётся прежним (по имени).
ALTER TABLE langpack_languages ADD COLUMN position INTEGER NOT NULL DEFAULT 0;

-- +goose Down
ALTER TABLE langpack_languages DROP COLUMN position;

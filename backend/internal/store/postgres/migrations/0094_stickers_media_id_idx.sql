-- +goose Up
-- stickers.media_id стал горячим путём, а индекса на него не было: 0051
-- проиндексировал только set_id. По media_id фильтруют три запроса, и все
-- вызываются на пользовательских действиях, а не в фоне:
--   • StickersRepo.SetByMediaID — на каждый клик по стикеру в чате (резолв
--     набора для попапа);
--   • StickersRepo.IsStickerMedia — на каждую отправку стикера (usecase/chat/
--     message.go) и на каждую проверку доступа к медиа-файлу стикера
--     (usecase/chat/reaction.go), то есть на каждую его загрузку.
-- Строк в таблице после заливки наборов Telegram ~13 тыс. — без индекса каждый
-- такой вызов идёт seq scan'ом по всей таблице.
CREATE INDEX stickers_media_id_idx ON stickers(media_id);

-- Здесь же раньше удалялся sticker_sets_rank_idx как заведомо неиспользуемый:
-- единственный запрос, сортирующий по rank, — StickersRepo.FeaturedSets с
-- `ORDER BY (s.rank = 0), s.rank, s.id DESC`, где ведущий ключ сортировки —
-- ВЫРАЖЕНИЕ `(s.rank = 0)`, а не колонка, поэтому btree по rank порядок не
-- отдаёт и планировщик его не возьмёт ни при каком плане. Приводить запрос к
-- индексируемому виду не стали: наборов сотни (сейчас 338), полная сортировка
-- такой таблицы дешевле поддержки частичного индекса ради плана, который на
-- этом объёме всё равно останется seq scan + sort.
--
-- Сам DROP убран: создававшая индекс миграция переехала с 0093 на 0097
-- (коллизия версий с 0093_discussion_mirrors из параллельной ветки) и теперь
-- идёт ПОСЛЕ этой — на чистой базе DROP падал бы на несуществующем индексе.
-- Индекс больше не создаётся вовсе, удалять нечего.

-- +goose Down
DROP INDEX stickers_media_id_idx;

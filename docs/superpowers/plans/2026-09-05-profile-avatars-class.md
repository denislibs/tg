# Шапка профиля классом `PeerProfileAvatars` — план реализации

> **Для агентов:** ОБЯЗАТЕЛЬНЫЙ СУБ-СКИЛЛ: `superpowers:subagent-driven-development`.
> Шаги помечены чекбоксами (`- [ ]`).

**Цель:** заменить самодельную React-карусель аватаров внутри `UserInfoPanel.tsx`
дословным портом класса tweb `src/components/peerProfileAvatars.ts`, и заодно
свести два разошедшихся механизма сворачивания шапки в один.

**Место в программе:** волна 3 Solid-миграции
(`docs/superpowers/specs/2026-08-28-solid-migration-design.md`), этап 2 (профиль),
**шаг 1 из двух**. Шаг 2 — Solid-порт `peerProfile.tsx` — пишется отдельным планом
после закрытия этого. Порядок именно такой, потому что в оригинале имя и статус
пира живут ВНУТРИ узла карусели (`avatars.info`, tweb `peerProfile.tsx:217-272`):
сделать карточку первой значило бы собрать её вокруг нашей самоделки и потом
переделать.

**Архитектура:** карусель в tweb — императивный класс, а не Solid-компонент.
Портируем его классом же (`web-client/src/components/peerProfileAvatars.ts`),
зависимости он получает через конструктор (как `components/avatar.ts:104`
получает `managers`), в React-панель встаёт через `core/hooks/useImperativeIsland`.
После этого шага React владеет только оболочкой панели (портал, шапка сайдбара,
оверлеи) — узлом шапки-аватаров владеет класс.

**Оригинал:** `/Users/denisurevic/Documents/tweb/src/components/peerProfileAvatars.ts`
(974 строки). Разбор устройства с адресами — `docs/tweb/right-sidebar.md` § 3.3.

## Global Constraints

- **Источник порта — tweb, а не наш React.** В каждом коммите — ссылки
  `file:line` на оригинал для каждого портированного поведения (DoD п.1).
  Формулировка «сохрани текущее поведение» запрещена: так в порт протаскиваются
  наши недоделки.
- **Расхождение либо отсутствует, либо объявлено комментарием ПРЯМО У СТРОКИ**
  с причиной и адресом оригинала (DoD п.2). Молчаливых расхождений нет.
- **Норма проводки:** каждая строка проводки обязана краснить тест, если её
  испортить. В теле КОММИТА — транскрипт мутаций: что испортил, какой тест
  покраснел, с именем теста и реальным выводом vitest (DoD п.3, п.4).
- Комментарии по-русски, по стилю соседних файлов.
- React внутри класса не появляется. `framer-motion`, MUI не возвращать.
- Мёртвый код удалять сразу: у снесённых компонентов не остаётся ни файла, ни
  типов, ни хуков-питателей без потребителей.
- Работа только в ворктри
  `/Users/denisurevic/Documents/messenger-denis/.claude/worktrees/solid-wave-3`,
  ветка `feat/profile-avatars-class`. Контейнеры не поднимать, конфиги
  (docker-compose, nginx, vite, vitest) не трогать, проверки не обходить.
- Индексировать только свои пути; индексация всего дерева запрещена (индекс
  ворктри общий с фоновыми агентами).
- Перед словом «готово» в КАЖДОЙ задаче: `npx vitest run` (весь набор, не
  выборочно), `npx tsc --noEmit`, `npx vite build` — все три из `web-client/`.

## Что НЕ портируем в этом шаге (и почему)

Каждый пункт — объявленное отступление: комментарий у места в коде + файл долга
в `web-client/backlogs/frontend/` (заводит задача 6).

| Механика tweb | Строки | Почему не сейчас |
|---|---|---|
| История фото чата/канала (`getHistory` с `inputMessagesFilterChatPhotos`) | :443-499 | На бэкенде нет ручки: `GET /users/{id}/photos` есть только для пользователя (`backend/.../profile_handler.go:439-460`). Карусель для групп/каналов остаётся одним фото. Правится БЭКЕНДОМ (DoD п.2a), задача в бэклоге |
| Пагинация списка фото | :433-521 | Та же ручка отдаёт весь список одним ответом, курсора нет. `ListLoader` подключаем как в оригинале, но `loadMore` отдаёт единственную страницу |
| Фон-паттерн профиля (`wrapEmojiPattern`, `usePeerProfileAppearance`) | :652-793 | Нет ни `wrapEmojiPattern`, ни поля `profile_color` в наших данных |
| Кольца историй (`StoriesSegments`) | :852-880 | Подсистемы историй нет вовсе |
| Прогресс загрузки своей аватарки (`avatarUploads`-стор) | :545-590 | Стора нет; свой аватар грузится другим путём |
| Fallback-фото («публичное» фото своего профиля) | :422-431 | Понятия fallback-фото в наших данных нет |

## Структура файлов

- Создать: `web-client/src/components/peerProfileAvatars.ts` — класс.
- Создать: `web-client/src/components/peerProfileAvatars.test.ts` — поведение класса.
- Изменить: `web-client/src/components/UserInfoPanel.tsx` — снос инлайновой
  карусели (:451-526), `AvatarVideo` (:47-70), инлайнового сворачивания
  (`onBodyScroll` :170, `onBodyWheel` :191), встраивание класса.
- Изменить: `web-client/src/components/UserInfoPanel.shell.test.ts` — текстовые
  пины ссылаются на снесённые строки.
- Удалить: `web-client/src/core/photoPager.ts` + `photoPager.test.ts` (в tweb
  такого модуля нет: индекс, зоны и решение по свайпу живут в самом классе).
- Удалить: `useProfilePhotos` и тип `HeaderPhoto` из
  `web-client/src/core/hooks/useUserProfileData.ts`, если после задачи 5 у них
  не осталось потребителей (проверить грепом, не на глаз).
- Изменить: `docs/tweb/right-sidebar.md` — секции «у нас».
- Создать: файлы долгов в `web-client/backlogs/frontend/`.

---

### Задача 1: каркас класса, DOM и классы состояний

**Файлы:**
- Создать: `web-client/src/components/peerProfileAvatars.ts`
- Создать: `web-client/src/components/peerProfileAvatars.test.ts`

**Интерфейсы:**
- Отдаёт: `export default class PeerProfileAvatars` с публичными
  `container: HTMLElement`, `info: HTMLElement`, `setPeer(peerId, threadId?)`,
  `goWithoutTransition(distance: number)`, `cleanup()`,
  `onNeedWhiteChanged?: (needWhite: boolean) => void`; конструктор принимает
  `{ managers, setCollapsedOn: HTMLElement, scrollableEl: HTMLElement }` —
  зависимости через конструктор, как у `components/avatar.ts:104`.

- [ ] **Шаг 1: прочитать оригинал** — `peerProfileAvatars.ts:41-140` (поля,
  конструктор, DOM), `:795-805` (`addTab`), `:929-955` (`setCollapsed`,
  `isCollapsed`, `updateHeaderFilled`), `:957-973` (`cleanup`). Плюс
  `docs/tweb/right-sidebar.md` § 3.3 и § 5.1.

- [ ] **Шаг 2: написать падающий тест** на DOM и переключение классов
  (`peerProfileAvatars.test.ts`): контейнер `.profile-avatars-container` с
  детьми в порядке оригинала (`-avatars`, `-gradient`, `-gradient-top`,
  `-tabs`, `-arrow`, `-arrow-next`, `-info`); `addTab()` добавляет
  `.profile-avatars-tab`; `setCollapsed(true|false)` вешает/снимает
  `is-collapsed` на переданный `setCollapsedOn`, а НЕ на свой контейнер;
  `header-filled` появляется по порогу прокрутки (5px без цветного фона,
  200px иначе — tweb `:949-955`); `cleanup()` не оставляет ни узлов, ни
  слушателей.

- [ ] **Шаг 3: убедиться, что тест падает.**
  Запуск: `npx vitest run src/components/peerProfileAvatars.test.ts`

- [ ] **Шаг 4: портировать каркас.** Константы `BASE_CLASS = 'profile-avatars'`,
  `SCALE = 1`, `TRANSLATE_TEMPLATE = 'translate({x}, 0)'`, `LOAD_NEAREST = 3` —
  дословно (`:38-44`). Ветка топика (`is-topic`, `:396-400`) портируется: один
  статичный аватар, без карусели.

- [ ] **Шаг 5: тесты зелёные + мутации.** Испортить по очереди: порядок узлов,
  цель `setCollapsed` (свой контейнер вместо `setCollapsedOn`), пороги
  `header-filled`, снятие слушателей в `cleanup` — записать вывод vitest.

- [ ] **Шаг 6: коммит** с транскриптом мутаций в теле и ссылками на оригинал.

---

### Задача 2: данные и лента фото (`ListLoader` + `processItem`)

**Файлы:**
- Изменить: `web-client/src/components/peerProfileAvatars.ts`
- Изменить: `web-client/src/components/peerProfileAvatars.test.ts`

**Интерфейсы:**
- Потребляет: `ListLoader` — наш порт
  `web-client/src/components/mediaViewer/listLoader.ts` (207 строк:
  `loadCount`/`loadWhenLeft`/`go`/`load`/`setTargets`/`reset`); данные —
  `managers.profile.listPhotos(peerId)`
  (`web-client/src/core/managers/profileManager.ts:110`, ручка
  `GET /users/{id}/photos`, отдаёт весь список разом, только для пользователей).
- Отдаёт: заполненную ленту `.profile-avatars-avatar` и синхронные с ней
  полоски-индикаторы.

- [ ] **Шаг 1: прочитать оригинал** — `:376-543` (`setPeer`, сборка
  `ListLoader`), `:500-520` (`onJump`), `:807-912` (`processItem`),
  `:898-927` (ленивая догрузка через `IntersectionObserver` + `LOAD_NEAREST`).

- [ ] **Шаг 2: падающий тест.** На фейковом `managers.profile.listPhotos`:
  N фото → N узлов `.profile-avatars-avatar` и N полосок;
  `goWithoutTransition(+1)` двигает `avatars.style.transform` на
  `translate(-100%, 0)` и переносит `.active` и на аватаре, и на полоске;
  закольцовывание на краях; смена пира сбрасывает ленту (старых узлов не
  остаётся); ответ на ПРОТУХШИЙ peerId игнорируется (гонка «переключили пира,
  пока грузилось»).

- [ ] **Шаг 3: убедиться, что тест падает.**

- [ ] **Шаг 4: реализация.** `loadMore` возвращает единственную страницу —
  у самой строки комментарий с адресом оригинала (`:433-521`) и причиной
  (ручка без курсора) плюс ссылка на файл долга из задачи 6. Для не-пользователя
  (группа/канал) — одно фото, тем же путём, с комментарием на месте ветки
  `:443-499`. Видео-аватар отрисовывается по `videoMediaId`; регистрация видео
  в `animationIntersector` — как в оригинале (`:591-650` даёт только регистрацию
  и снятие; rAF-цикл прогресса полоски идёт задачей 3).

- [ ] **Шаг 5: тесты зелёные + мутации** (сорвать `.active`, сорвать сброс при
  смене пира, сорвать отбрасывание протухшего ответа).

- [ ] **Шаг 6: коммит.**

---

### Задача 3: жесты — зоны клика, стрелки, свайп через `SwipeHandler`

**Файлы:**
- Изменить: `web-client/src/components/peerProfileAvatars.ts`
- Изменить: `web-client/src/components/peerProfileAvatars.test.ts`
- Удалить: `web-client/src/core/photoPager.ts`, `web-client/src/core/photoPager.test.ts`

**Интерфейсы:**
- Потребляет: `SwipeHandler` — наш порт `web-client/src/core/dom/swipeHandler.ts`
  (549 строк).

- [ ] **Шаг 1: прочитать оригинал** — `:127-137` (`checkScrollTop`), `:142-233`
  (клик: зоны `SWITCH_ZONE = 1/3`, центр → просмотрщик, свёрнутое состояние →
  `unfold`), `:243-298` (свайп: `onFirstSwipe`/`onSwipe`/`onReset`, классы
  `is-swiping`/`no-transition`), `:591-650` (rAF-цикл прогресса полоски у
  видео-аватара).

- [ ] **Шаг 2: падающий тест.** Клик в левую/правую треть листает и
  закольцовывается; клик в центр открывает просмотрщик (мок
  `components/mediaViewer/openMediaViewer.ts`) с текущим индексом; клик при
  `is-collapsed` только разворачивает и НЕ листает; свайп мимо порога
  возвращает ленту на место, за порогом — переводит индекс;
  `.is-swiping`/`.no-transition` появляются и снимаются; rAF-цикл прогресса
  сам останавливается, когда видео на паузе.

- [ ] **Шаг 3: убедиться, что тест падает.**

- [ ] **Шаг 4: реализация.** Свайп — только через портированный `SwipeHandler`;
  голых `pointerdown/move/up` в классе быть не должно.

- [ ] **Шаг 5: снести `core/photoPager.ts` и его тест.** В tweb такого модуля
  нет — индекс, зоны и решение по свайпу живут в самом классе. Если на момент
  задачи 3 `UserInfoPanel.tsx` ещё импортирует `photoPager`, снос переносится
  в задачу 5 — тогда написать об этом строку в отчёте, а не удалять наполовину.

- [ ] **Шаг 6: тесты зелёные + мутации** (порог свайпа, границы зон, гейт
  `is-collapsed`, остановка rAF).

- [ ] **Шаг 7: коммит.**

---

### Задача 4: сворачивание шапки — один механизм вместо двух

**Файлы:**
- Изменить: `web-client/src/components/peerProfileAvatars.ts`
- Изменить: `web-client/src/components/UserInfoPanel.tsx`
- Изменить: `web-client/src/core/hooks/useCollapsable.ts` — только если
  понадобится расширить контракт; трогать бережно: у хука есть второй
  потребитель `components/StoriesRow.tsx` и свой тест
  `core/hooks/useCollapsable.test.tsx`.

**Интерфейсы:**
- Потребляет: `useCollapsable` (`web-client/src/core/hooks/useCollapsable.ts`,
  229 строк) — наш порт tweb `src/hooks/useCollapsable.ts` (209).
- Отдаёт: класс получает `unfold`/`fold` снаружи и отдаёт наружу факт
  сворачивания; двух реализаций одного механизма в дереве не остаётся.

**Объявленное отступление (записать комментарием у места):** в tweb хук — на
Solid и живёт ВНУТРИ класса (`peerProfileAvatars.ts:322-348`: `createRoot` +
`createEffect(folded → setCollapsed)`). Наш `useCollapsable` — React-хук
(`useState`/`useRef`, строка 21), потому что писался под React-потребителя
`StoriesRow`. Поэтому на этом шаге хук зовёт React-оболочка панели и толкает
состояние в класс. Сведение к устройству оригинала — в шаге 2 этапа (Solid-порт
`peerProfile.tsx`), где владельцем станет Solid-компонент; долг записать файлом
в бэклоге (задача 6) и сослаться на него у строки.

- [ ] **Шаг 1: прочитать** `peerProfileAvatars.ts:312-348` (подписка на
  дополнительный скролл через `fastRaf` и связка с `useCollapsable`), наш
  `core/hooks/useCollapsable.ts` целиком и текущие
  `UserInfoPanel.tsx:170-200` (инлайновые `onBodyScroll`/`onBodyWheel`).

- [ ] **Шаг 2: падающий тест.** Колесо вверх при `scrollTop === 0`
  разворачивает шапку, вниз — сворачивает; прокрутка ленты сворачивает
  немедленно; `header-filled` взводится по порогу И СНИМАЕТСЯ возвратом к
  началу (сегодня в панели он только взводится — `UserInfoPanel.tsx:184-190`;
  это наша недоделка, а не поведение оригинала: там `updateHeaderFilled`
  двусторонний, `:949-955`).

- [ ] **Шаг 3: убедиться, что тест падает** — в том числе на снятии
  `header-filled`: этот тест обязан краснеть ДО правки.

- [ ] **Шаг 4: реализация** — инлайновые `onBodyScroll`/`onBodyWheel` в
  `UserInfoPanel.tsx` снести, гейт колеса взять из хука.

- [ ] **Шаг 5: тесты зелёные + мутации.**

- [ ] **Шаг 6: коммит.**

---

### Задача 5: встраивание в панель и снос самоделки

**Файлы:**
- Изменить: `web-client/src/components/UserInfoPanel.tsx`
- Изменить: `web-client/src/components/UserInfoPanel.shell.test.ts`
- Изменить: `web-client/src/core/hooks/useUserProfileData.ts` (снятие
  `useProfilePhotos`/`HeaderPhoto`, если потребителей не осталось)

**Интерфейсы:**
- Потребляет: `core/hooks/useImperativeIsland`
  (`web-client/src/core/hooks/useImperativeIsland.ts`) — мост React→vanilla:
  запуск в layout-фазе до пейнта, `middleware`-scope, режимы уборки
  `own`/`host`. Читать докблок файла целиком; мост руками не писать.

- [ ] **Шаг 1: падающий тест на шов.** Пин обязан краснеть, если вызов класса
  вынести из эффекта или потерять уборку: после размонтирования панели узлов
  класса в DOM нет и слушателей нет. Образец пина на шов —
  `src/App.authMount.test.ts` (привязка по балансу скобок внутри тела эффекта,
  а не по факту наличия строки).

- [ ] **Шаг 2: убедиться, что тест падает.**

- [ ] **Шаг 3: встроить класс** через `useImperativeIsland`; снести инлайновую
  карусель (`UserInfoPanel.tsx:451-526`), `AvatarVideo` (:47-70), импорт
  `photoPager` (:33) и сам модуль с тестом, если он не был снесён задачей 3.

- [ ] **Шаг 4: снять питателя без потребителей.** Проверить грепом, остались ли
  потребители у `useProfilePhotos`/`HeaderPhoto`; если нет — удалить, не
  оставлять «на будущее».

- [ ] **Шаг 5: поправить `UserInfoPanel.shell.test.ts`** — текстовые пины
  указывают на снесённые строки. Пин не ослаблять: то, что раньше держалось
  текстом внутри панели, теперь держится поведенческими тестами класса —
  назвать это в комментарии теста.

- [ ] **Шаг 6: три проверки + мутации + коммит.**

---

### Задача 6: долги, документация, живая проверка

**Файлы:**
- Создать: `web-client/backlogs/frontend/profile-chat-photo-history.md`,
  `profile-appearance-emoji-pattern.md`, `profile-avatar-upload-progress.md`,
  `collapsable-solid-owner.md`
- Изменить: `docs/tweb/right-sidebar.md` (секции «у нас» в § 3.3, Часть 3 § 1.1 и § 4)

- [ ] **Шаг 1: файлы долгов** — по образцу
  `web-client/backlogs/frontend/tracking-monkey-solid-port.md`: что в tweb,
  чего не хватает у нас, что именно делать, критерий готовности. Для истории
  фото чата — с указанием, что правится БЭКЕНДОМ (новая ручка), а не подгонкой
  клиента (DoD п.2a).

- [ ] **Шаг 2: обновить `docs/tweb/right-sidebar.md`** — секции «у нас»
  привести к факту (карусель классом, единый механизм сворачивания, что не
  портировано).

- [ ] **Шаг 3: живая проверка на стенде** (`https://localhost:38443`,
  `?noSharedWorker=1`, dev-OTP `12345`): открыть профиль пользователя с
  несколькими фото — листание кликом по краям, свайпом, стрелками;
  разворачивание и сворачивание шапки колесом и скроллом; заливка шапки при
  прокрутке и снятие при возврате; открытие просмотрщика кликом по центру;
  видео-аватар (проигрывание и прогресс полоски); профиль группы и канала
  (одно фото, карусели нет); секретный чат; переключение между пирами без
  перезагрузки. Результат — числами и фактами в отчёте; «выглядит нормально»
  не принимается.

- [ ] **Шаг 4: коммит.**

---

## Самопроверка плана

- **Покрытие:** DOM и классы состояний — задача 1; данные и лента — 2; жесты —
  3; сворачивание — 4; шов с панелью и снос самоделки — 5; долги, документация,
  живая проверка — 6. Непортируемые механики перечислены таблицей выше, у
  каждой — файл долга в задаче 6.
- **Заглушек нет:** в каждой задаче названы файл, строки оригинала, предмет
  теста и предмет мутации.
- **Согласованность имён:** `container`/`info`/`setPeer`/`goWithoutTransition`/
  `cleanup`/`onNeedWhiteChanged` — имена оригинала, объявлены в задаче 1 и
  используются дальше без переименований.

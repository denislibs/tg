# Task 14 — AppMediaViewer: message-вариант + навигация

Статус: **готово**. Коммит `ddf8347a` (ветка `feat/tweb-media-core`), не запушено.

## Что сделано

### `web-client/src/components/mediaViewer/appMediaViewer.ts` (новый)

Порт tweb `src/components/mediaViewer/index.ts` (класс `AppMediaViewer`,
message-вариант) в объёме Task 14:

- **Класс** `AppMediaViewer extends AppMediaViewerBase<'caption', 'delete' | 'forward', ViewerTarget>`;
  `topButtons ['delete','forward']` — итоговый порядок кнопок
  delete, forward, download, rotate, zoomin, close (пин уже в `base.test.ts`).
  Конструктор: обработчик delete (tweb :145), ⋮-меню, статичная видимость
  действий, `onAuthorClick`, `setListeners()` (tweb :166).
- **Модель данных**: `ViewerItem` (аналог item старого `useLightbox` + `mid`:
  element/mid/media/author/caption/captionEntities), `ViewerTarget`
  (порт `AppMediaViewerTargetType` :39-45 — element+mid+item вместо
  peerId/message). `getMediaFromMessage`/RPC managers не нужны — item собирает
  вызывающий (Task 16).
- **openMedia** (порт :420-514): вход `{items, index, target, fromRight, reverse}`;
  `setCaption` до `_openMedia` (tweb :468); первый вызов кладёт соседей вокруг
  index в listLoader (`setTargets` внутри base `_openMedia`); после — дозапись
  `target.mid/item` (tweb :484-487). `permissionsPromise` (:457-498) не
  портирован — видимость действий статична по колбэкам.
- **Навигация**: `listLoader.onJump → onPrevClick/onNextClick` (tweb base
  :485-489, вешается в подклассе — поля подкласса), повторный `openMedia`
  соседа с `fromRight ±1` (порт :204-220; фетч `getMessageByPeer` не нужен).
- **Caption**: `setCaption` (порт :304-356 в объёме) — RichText-остров через
  `renderCaptionIsland` (hide на пустой — tweb :354). Спонсорские
  сообщения/TranslatableMessage/videoTimestamps — не портированы (фич нет /
  Task 15).
- **Действия** — инжектируемые колбэки `opts`:
  - `onForward(mid, close)` / `onDelete(mid, closeFromMedia)` — React-попапы
    придут в Task 16; `closeFromMedia` перецеливает close в отцентрованное
    медиа (tweb :249-252). Кнопка без колбэка получает `hide` — порт
    `setMessageActionVisibility` (:396-418) на DOM-кнопки и пункты меню.
  - `jumpToMessage(mid)` — клик по автору: `close().then(() => jump(mid))`
    (порт :268-290; ветка мобильного shared-media-таба не портирована).
  - download — внутренняя существующая механика (как `MediaLightbox.download`
    / DocRow): `downloadMediaURL` + `meta().fileName` + `<a download>`.
- **Пагинация**: `loadMoreMedia?: (older, anchor, loadCount) => Promise<ViewerItem[]>`
  → `listLoader.loadMore`; без колбэка пустой ответ короче `loadCount` ⇒
  `loadedAll` (листаются только переданные items, как старый лайтбокс).
  Префетч-порог base (`next < 10 → load(true)` после конца полёта) состыкован —
  проверено тестом.
- **Мобильное ⋮-меню**: порт `setBtnMenuToggle` (base :970-973) + минимальный
  vanilla-объём `ButtonMenuToggle`/`ButtonMenu` (кнопка
  `btn-icon.only-handhelds.btn-menu-toggle` с глифом more, меню
  `.btn-menu.bottom-left` из `.btn-menu-item.rp-overflow` — классы открытия
  `active`/`was-open`/`menu-open` как `contextMenuController` :140-142).
  Стили — уже портированный `styles/tweb/_button.scss`. Меню живёт внутри
  кнопки (штатная форма tweb — `ButtonMenuToggleHandler:43`);
  `positionMenuTrigger`/монтирование в body — доводка Task 17 (пометка в коде).

### `base.ts` (правки под подкласс)

- `target` — геттер/сеттер поверх `listLoader.current` (порт tweb :290-296
  вместо собственного поля): `go()` перекладывает current между previous/next,
  без этого просмотренный элемент выпадал бы из списка при листании.
  В `_openMedia` — `this.target = { element } as TargetType` (tweb :2400 `as any`).
- Экспортированы `btnIcon`/`iconSpan` (пункты меню и кнопка more подкласса).
- Актуализированы стыковочные комментарии Task 14.

### `HotkeysSettings.tsx` (п. 5)

Секция Media Viewer сверена с фактом порта (base `onKeyDown`: стрелки вне
зума, зум `Ctrl+=`/`Ctrl+-`): текст уже говорил Ctrl+± (рассинхрон был со
старым MediaLightbox, слушавшим голые +/−; он умирает в Task 16) — приведены
метки клавиш 1:1 с tweb `keyboardShortcuts.tsx:24-25` (`plus: '+'`,
`minus: '−'`) и ссылка в комментарии (MediaLightbox → mediaViewer/base.ts).

## Тесты

`appMediaViewer.test.ts` — 15 тестов (среда как `base.open.test.ts`):
раскладка prev/next + reverse, hide на краю, клик next/prev → `openMedia`
с `fromRight ±1` и соседним mid, caption-остров (рендер/hide при листании),
видимость и кликабельность forward/delete по колбэкам, `closeFromMedia`
(перецел в `content.media` + закрытие), close→jumpToMessage (порядок пином:
jump видит вьювер уже снятым с DOM), download → `downloadMediaURL(mediaId)` +
`<a download=fileName>`, префетч `loadMoreMedia(older=true, anchor, 50)` +
дозаполнение next, loadedAll без колбэка, ⋮-меню (дерево/классы, открытие/
закрытие, пункт зовёт действие и закрывает).

**Прогоны**: `npx vitest run` — **230 файлов, 1605 passed | 2 skipped**;
`npm run typecheck` — чисто; oxlint — в тронутых файлах 0 предупреждений
(дельта 0; ловил 3 unbound-method — исправлено стрелочным свойством);
`npx vite build` — успешно.

## Мутации (реальный вывод vitest)

1. **Знак fromRight листания** (`onNextClick: fromRight 1 → -1`):
   `15 tests | 10 failed`, первым — целевой
   `× клик next зовёт openMedia соседа с fromRight=1; prev — с fromRight=-1`.
2. **Срезы соседей** (`prevTargets ↔ nextTargets` в openMedia):
   `15 tests | 12 failed`, в т.ч. целевые
   `× первый вызов кладёт prev = items до index, next = после` и
   `× на краю списка соответствующий свитчер получает hide`.
3. **Порядок close→jump** (jump до close): `15 tests | 1 failed` — ровно
   целевой `× jumpToMessage зовётся с mid ПОСЛЕ реального закрытия`,
   `AssertionError: expected true to be false` (вьювер ещё в DOM).

Все мутации откачены точечной обратной заменой; финальный прогон зелёный.

## Замечания для следующих задач

- Task 15 (видео): `openMedia` уже прокидывает `kind: 'video'` в base-заглушку;
  плеер встанет в `_openMedia`-ветку isVideo.
- Task 16 (точки входа): собрать `ViewerItem[]` из окна сообщений (модель
  useLightbox + mid), передать `onForward`/`onDelete` (React-попапы),
  `jumpToMessage`, `loadMoreMedia` (REST `/chats/{id}/media`), Esc через
  navLayer; снести `MediaLightbox.tsx`/`useLightbox.ts`.
- Task 17: доводка ⋮-меню (`positionMenuTrigger`, монтирование в body,
  ripple пунктов) — пометка в `setBtnMenuToggle`.
- `onEmptied` tweb (текущий элемент удалён → close) не портирован — наш
  ListLoader события не знает; удаление закрывает вьювер через `closeFromMedia`.

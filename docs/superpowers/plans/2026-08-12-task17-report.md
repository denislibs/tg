# Task 17 — финал медиа-суперпорта (зачистка, сверки, CLAUDE.md)

**Статус: выполнено.** Коммиты `926b381a` (chore(viewer) — хвосты) и
docs-коммит следом (CLAUDE.md + этот отчёт). Ветка `feat/tweb-media-core`,
не запушено.

## 1. Хвосты из отчётов Task 12/14/15/16

- **(а) Словарь.** Единственный реально отсутствовавший ключ вьювера/плеера —
  `Normal` (пункт «обычная скорость» меню скоростей, `lib/mediaPlayer/index.ts:451`);
  добавлен в `dict.ru.ts` — `'Обычная'` (официальная строка Telegram).
  `Forward`/`Download`/`Delete` уже были («Переслать»/«Загрузить»/«Удалить»).
  Остальные словари (uk/es/de/fr) системно тоньше — нет даже `Play`/`Pause`;
  по конвенции словаря (`i18n/dict.ts`: en-ключ = фолбэк) оставлены без правок.
- **(б) ⋮-меню — positionMenuTrigger.** Новый вендор-порт
  `src/helpers/positionMenu.ts` — ТОЛЬКО `positionMenuTrigger`
  (tweb `helpers/positionMenu.ts:315-341`; остальной файл — контекстные меню от
  точки клика — не портирован, наши контекстные меню — React `shared/ui/Menu`).
  `appMediaViewer.setBtnMenuToggle` доведён до tweb-цикла autoPosition
  (`buttonMenuToggle.ts:145-186`): на открытие меню монтируется в
  `getOverlayRoot()` (body) и позиционируется инлайн-координатами от rect
  кнопки (`bottom-left`, паддинг `{top: 8, bottom: 8}` — tweb :157), классы
  открытия — после `doubleRaf` (кадр на transition, tweb :158 + openBtnMenu);
  закрытие снимает узел из DOM отложенно (300 мс — уход transition, tweb
  :171-186), роль tempId tweb — поколение открытия. `close()`/`destroy()`
  вьювера снимают body-узел (в tweb это работа contextMenuController — у нас
  контроллера нет, помечено). z-index: `_bridge.scss` дополнен
  `body > .btn-menu { z-index: 4000 }` — продолжение задокументированного
  отступления Task 16 (tweb: вьювер 4 и btn-menu 4, решает DOM-порядок; у нас
  вьювер поднят до 4000 — равное значение возвращает ту же механику).
- **(в) `getZoomBounce` снесён** (`base.ts`). Проверено по tweb: метод объявлен
  (`base.ts:791-793`) и не вызывается нигде в репозитории — мёртв и в
  референсе; по норме «мёртвый код удалять» удалён, на месте — комментарий.
  Поле `isGesturingNow` (его единственный читатель) стало write-only; записи —
  часть 1:1-портированных тел `onSwipeFirst`/`onSwipeReset`, поле оставлено с
  пометкой (не форкать тела ради поля).
- **(г) Плашка длительности shared-грида.** Проверено по tweb
  `appSearchSuper.ts`: ячейка грида — `div.grid-item` (:878), плашку
  `.video-time` внутри строит `wrapVideo`. Значит «ближе к tweb» — классы на
  разметке, НЕ новый контекст в FLOATING_CONTEXTS (контекст `grid-item` уже
  портирован): `SharedMedia.tsx` — ячейка получила `grid-item`, плашка —
  `video-time` (визуал остался на классах модуля: tweb-стили `.video-time`
  скоуплены под `.bubble` и на грид не действуют). Плашка теперь гаснет на
  полёте мувера (`hideFloatings`).

## 2. Сверка-счётчики

### 2а. `components/mediaViewer/base.ts` против tweb `mediaViewer/base.ts`

Прогнан весь список членов класса tweb (58 методов/геттеров + поля) против
нашего файла. **Портировано: 53 из 58. Непортированных членов — 5** (+1 снесён
как мёртвый, 1 перенесён в подкласс):

| tweb-член (строки) | Статус | Причина |
|---|---|---|
| `get hasQualityOptions` (:238-240) | не портирован | HLS/quality-меню: у нашего бэка один полноразмер, HLS-качеств не существует (помечено в шапке base.ts:179) |
| `removeQualityOptions` (:2276-2278) | не портирован | то же (HLS) |
| `loadQualityLevelsDownloadOptions` (:2280-2318) | не портирован | то же (HLS, download-меню качеств) |
| `setMediaTimestamp` (:1055-1063) | не портирован | видео-таймстампы в подписи (t.me-ссылки `?t=`) — фичи нет; помечено в appMediaViewer (saveTimestamps/videoTimestamps) |
| `sizeTailPath` (:1884-1910) | не портирован | SVG-хвостовые баблы — у наших баблов SVG-хвостов нет; все 4 точки вызова помечены на месте (base.ts:1660, 1724, 1827, 1916, 2001) |
| `getZoomBounce` (:791-793) | **снесён (Task 17)** | мёртв и в tweb — объявлен, не вызывается |
| `setBtnMenuToggle` (:970-973) | перенесён в подкласс | у tweb — generic по buttons в базе; наш вариант живёт в `appMediaViewer.ts` (единственный потребитель), без verify-механики ButtonMenu |

Непортированные ВЕТКИ внутри портированных методов — каждая помечена
комментарием у строки (грепается по «не портирован» в base.ts):

| Ветка | Где помечено | Причина |
|---|---|---|
| live-стрим RTMP: `onClick` (PiP по клику в фон, admin-popup-container), канвас-ветка `this.live` (DPR=1 + boxBlurCanvasRGB), поля `adminPanel`/`streamEnded` | шапка файла; :1271, :1750 | RTMP-стримов не существует |
| HLS: `initVideoHls`/`toggleStreamInUse`, `downloadQualityMenuOptions` | :179 | HLS-качеств нет |
| SVG-хвосты: измерение цели `SVGImageElement`/`SVGForeignObjectElement`, `path`/`isOut`, use/path `generatePathData` | :1513, :1724, :1798 | SVG-хвостовых баблов нет |
| profile-avatars-морф: цель `profile-avatars-avatar` (:1300-1308), FLOATING_CONTEXTS-контекст `profile-avatars-container` (:2140-2144) | :1513, :2149 | шапка профиля на CSS-модулях без tweb-классов; аватар-вариант открывается через media.url (Task 16) |
| `overlayCounter.overlaysActive > 1` в onKeyDown/onKeyUp, `isDarkOverlayActive` | :1076 (и рядом) | оверлей-счётчика tweb нет; Esc/Back — слои контроллера openMediaViewer |
| `navigationItem`/`appNavigationController` (:2429-2450, :992-994) | шапка; :1074 | заменён контроллером openMediaViewer (pushEsc + pushLayer) |
| `noAutoDownload`-гейт, `fullPhotoSize` по `media.sizes`, `updateMediaSource(url)` :2948 | :2423, :2645, :2691 | автозагрузка всегда включена; один полноразмер у бэка |
| `SearchListLoader.cleanup`, `lazyLoadQueue`, `disposeSolid`, ripple mobile-close | :1072-1079; шапка | MTProto-поиска/solid-островов нет; ripple — React-хук |

### 2б. Партиалы стилей против tweb

- `styles/tweb/_mediaViewer.scss` ↔ tweb `components/mediaViewer/mediaViewer.scss`
  — **0 расхождений** (diff пуст, кроме строки `@use` foundation/shared).
- `styles/tweb/_preloader.scss` ↔ tweb `scss/partials/_preloader.scss` — **0**.
- `styles/tweb/_ckin.scss` ↔ tweb `scss/partials/_ckin.scss` — **0** (бонус-проверка).

Появившиеся за программу не-tweb правила вьювера собраны в одном месте —
`_bridge.scss` (z-index 4000 вьювера + `body > .btn-menu`), оба блока несут
обоснование.

## 3. CLAUDE.md (web-client)

- Новый раздел **«Медиа»**: (а) vanilla-вьювер — карта файлов
  (base/appMediaViewer/openMediaViewer, lib/mediaPlayer, preloader), React
  только островами createRoot (аватар автора, caption), правило «изменения
  ядра — сначала tweb, порт 1:1»; (б) медиа-слой — владелец URL воркер
  (`downloadMediaURL` → CacheStorage `cachedFiles`, ≤ `MAX_FILE_SAVE_SIZE`
  20 МиБ → objectURL → `rt:media_url`), зеркало `core/mediaCache.ts` +
  `useMediaUrl`/`useMediaThumb`, стирание на logout (`resetDownloads` +
  `deleteAll`); токен — ТОЛЬКО стрим DNP-OFF (`resolveStreamUrl`,
  инлайн-автоплей RealMediaBubble, mediaPlaybackController) и байтовые пути
  (StickerMedia, DocRow/файл-документ, waveform, secret/mediaCache).
- Пункт «МОЖНО: грузить медиа-bytes прямым fetch» актуализирован: явно про
  НЕ-картинки; упоминание мёртвого `useMediaContentUrl` убрано.
- Таблица «Владение фактами»: строка `медиа-токен` сужена (стрим DNP-OFF +
  байтовые пути), добавлена строка **URL медиа (objectURL)** с пином
  `core/noDuplicateMediaUrl.test.ts`.

## 4. Тесты и мутации

`appMediaViewer.test.ts`: 14 → **17** (обновлён тест дерева меню — узел до
открытия вне DOM; новые: «открытие монтирует в body и позиционирует»,
«повторный клик закрывает и уносит из DOM через 300 мс», «close() вьювера
снимает открытое меню»).

Мутации (все откачены точечной обратной заменой, вывод vitest реальный):

1. **`positionMenuTrigger` не зовётся** (`void positionMenuTrigger`):
   `× открытие монтирует меню в body и позиционирует от кнопки (positionMenuTrigger bottom-left)`
   → `Tests 1 failed | 16 passed (17)`.
2. **Отложенный `unmount` вырезан** (закомментирован `setTimeout(unmount, 300)`):
   `× клик по toggle открывает …, повторный — закрывает и уносит из DOM через 300мс`
   и `× close() вьювера снимает открытое меню (body-узел не сирота)`
   → `Tests 2 failed | 15 passed (17)`.
3. **`close()` не зовёт `closeBtnMenu`**:
   `× close() вьювера снимает открытое меню (body-узел не сирота)`
   → `Tests 1 failed | 16 passed (17)`.

## 5. Полная проверка

- `npx vitest run` — **234 файла, 1634 passed | 2 skipped** — зелёный.
- `npm run typecheck` (TS7 native) — чисто.
- oxlint — **214 warnings, дельта 0** (базлайн Task 15/16 — 214; в тронутых
  файлах предупреждений нет).
- `npx vite build --outDir /tmp/media-task17-build` — ✓ built (предупреждения
  чанкинга прежние).
- Бэкенд: `go build ./...` + `go vet ./...` — ок;
  `go test ./internal/usecase/media/... ./internal/adapter/media/...
  ./internal/usecase/auth/...` — ok (все три пакета).

## Замечания

- В `SearchView.tsx` тайлы `s.mediaTile` НЕ получили `grid-item`: клик там —
  `openMessage` (прыжок к сообщению), вьювер из выдачи поиска не открывается,
  полёта нет — контекст hideFloatings не нужен.
- Локализация uk/es/de/fr для строк вьювера/плеера — отдельная задача
  сплошного добора этих словарей (там нет и куда более базовых ключей).
- Ручная проверка на стенде из Task 16 не перепрогонялась: правки Task 17 —
  позиционирование меню (покрыто тестами), классы плашки и словарь.

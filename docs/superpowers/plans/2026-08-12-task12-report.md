# Task 12 — зум/пан/поворот + тач-разводка (Стадия E)

**Статус: выполнено.** Коммит `61112c8a` на `feat/tweb-media-core` (не запушено).

## Объём

Порт tweb `src/components/mediaViewer/base.ts` в наш
`web-client/src/components/mediaViewer/base.ts`:

1. **Две независимые transform-стопки.** Зум/пан/поворот живут на
   `moversContainer`, полёт — на мувере. `buildMoversTransform` (:856-873) с
   always-on rotate-обёрткой (identity при rotation=0 — комментарий-причина
   tweb сохранён: пофункциональная интерполяция первого поворота вместо
   matrix-декомпозиции), `applyMoversTransform`, `getMediaCenter`,
   `getDisplayRect`; `getRotationFitScale` был портирован ещё в Task 11.
2. **Зум-математика.** `onZoom` (:658-701, bounce headroom `ZOOM_MAX*3`),
   `calculateScaleOffset`, `getZoomBoundaries`, `calculateOffsetBoundaries`
   (isBetween+clamp), `changeZoomByPosition`, `setTransform`, `changeZoom`,
   `addZoomStep`/`addZoom`, `resetZoom`, `setZoomValue`, `toggleZoom`
   (`is-zooming` на whole, `is-visible` на zoom-container, `inactive` на
   кнопках, свап иконки локальным `replaceButtonIcon` — порт button.ts:48-53),
   `onSwipeFirst`/`onSwipeReset` (:590-656: инерция k=0.1, ignoreNextClick
   после mouse-drag), clamp-дебаунс 300 мс через наш `@helpers/schedulers/debounce`.
3. **Слайдер зума.** `web-client/src/components/rangeSelector.ts` — порт
   базового класса tweb `rangeSelector.ts` 1:1 (включая опции наследников
   Task 15: useTransform/useProperty/vertical/offsetAxisValue и `snapValue`);
   `web-client/src/helpers/dom/attachGrabListeners.ts` — порт 1:1. Зум-слайдер:
   step 0.01, min/max ZOOM_MIN/MAX, withTransition, onScrub →
   `addZoom(value − scale)`, onMouseDown/Up → onSwipeFirst/Reset (tweb :375-394).
4. **Поворот.** `rotateMedia` (−90°, identity-прайминг с no-transition только
   до первого инлайн-transform), `isRotated`, `resetRotationForNav()` — порт
   блока `_openMedia` :2416-2425 с пометкой «зовёт _openMedia, Task 13».
5. **Разводка SwipeHandler** (:522-587) на wholeDiv: 20%/125px → клики
   `buttons.prev/next` (обработчики кнопок :463-471 с гейтом `setMoverPromise`),
   вертикаль → `close()`, зум-пан через `adjustPosition` + `cancelDrag`,
   `verifyTouchTarget` (isFullScreen | zoom-container через findUpAsChild |
   `.ckin__controls` | caption | топбар кроме wheel), onZoom, onDoubleClick
   (в зуме reset, иначе scale 3 в точку).
6. **Клик и клавиатура.** `onClick` :1058-1130 без видео/PiP/live (тап на
   мобиле → `chrome-hidden`, `highlight-switchers` 3000 мс, click-outside →
   close по белому списку хрома); `onKeyDown`/`onKeyUp` :1132-1174 (стрелки
   вне зума, Ctrl+± → addZoomStep, ctrlKeyDown-трекинг);
   `setGlobalListeners`/`removeGlobalListeners`/`toggleGlobalListeners`
   :1036-1053 на window (у нас в PiP уходит только видео). Esc — пометка в
   шапке файла: tweb делает это appNavigationController'ом, наш navLayer —
   Task 16.

Новые хелперы (порты 1:1): `helpers/number/isBetween.ts`,
`helpers/dom/findUpAsChild.ts`, `helpers/dom/fullScreen.ts` (в объёме
isFullScreen; requestFullScreen и пр. — Task 15),
`helpers/dom/attachGrabListeners.ts`.

## Тесты

Полный прогон: **228 файлов, 1575 passed / 2 skipped** (`npx vitest run`).
Новых тестов 32 (было 1543):

- `base.zoom.test.ts` (14) — buildMoversTransform (точная строка с
  rotate(0deg)-обёрткой; fit-scale при −90°; пин двух стопок: transform на
  moversContainer, мувер нетронут), changeZoomByPosition (формула
  `p − scale·p` и кламп границами), onZoom (bounce 12 → clamp-дебаунс 300 мс
  → 4; scale<1 → resetZoom), toggleZoom (классы, inactive, свап иконки,
  скраб слайдера → addZoom), rotateMedia/resetRotationForNav, клавиатура.
- `base.touch.test.ts` (12) — свайп >125px гориз. → клик next/prev (и гейт
  setMoverPromise), верт. → close, в зуме → пан без close (+cancelDrag),
  инерция k=0.1 (точное −687), дабл-тап (вне зума scale 3 в точку / в зуме
  reset), verifyTouchTarget-белый список, chrome-hidden/highlight-switchers,
  ignoreNextClick.
- `rangeSelector.test.ts` (6) — разметка, onScrub из клика по треку
  (2.25 из середины; вычет step/10 левее половины), onInput, кламп filled,
  removeListeners.

`npm run typecheck` — чисто; oxlint — дельта 0 (все затронутые файлы без
находок); `npx vite build --outDir /tmp/media-task12-build` — успешно.

### Мутации (все краснят, вывод vitest)

1. **k инерции 0.1 → 0.5**:
   `AssertionError: expected -1024 to be -687 // Object.is equality` —
   `base.touch.test.ts > инерция пана: k = 0.1` (1 failed | 11 passed).
2. **rotate-обёртка при rotation=0 снята** (ранний return без обёртки):
   `AssertionError: expected 'translate3d(10.000px, 20.000px, 0px) …' to be …`
   и `… to contain 'rotate(0deg)'` — `base.zoom.test.ts` (2 failed | 12 passed).
3. **Порог 125px → 225px**:
   `AssertionError: expected false to be true` —
   `base.touch.test.ts > горизонтальный свайп > 125px` (1 failed | 11 passed).

Все мутации откачены точечной обратной заменой (Edit), не git checkout.

## Замечания

- **`close()` — заглушка** (ставит `closing`, снимает SwipeHandler; полный
  порт tweb :1075-1129 — Task 14). В классе полного close не было — только
  наш `destroy()`; заглушка помечена комментарием, её флаг `closing` уже
  читает onSwipeReset.
- **`getZoomBounce` мёртв и в tweb** (проверено grep'ом по всему tweb — метод
  объявлен, но нигде не вызывается). Портирован по границе задачи с
  комментарием «снести/оживить — решение сверения Task 17».
- **`updateVideoControlsLock`** (вызовы из toggleZoom/rotateMedia) не
  портирован — обслуживает только videoPlayer, помечен к Task 15.
- **Гейт `overlayCounter.overlaysActive > 1`** в onKeyDown/onKeyUp не
  портирован (оверлей-счётчика нет до Task 16, пометка на месте); resize →
  `applyLayoutVariables` в глобальных листенерах — придёт с Task 13.
- `clampZoomDebounced` создаётся в setListeners (как в tweb); до него
  хендлеры RangeSelector могут застать undefined — все вызовы через `?.`
  (в tweb strict выключен, прореха прикрыта частично; поведение то же).
- В `base.test.ts` обновлён один тест: `zoomElements.rangeSelector` теперь
  RangeSelector-инстанс (проверяется `container` + наполнение filled/seek).

# Task 10 — каркас `AppMediaViewerBase` (стадия E): отчёт

Ветка: `feat/tweb-media-core`. Коммит: см. `git log` (feat(viewer): каркас
AppMediaViewerBase). Референсы tweb: `src/components/mediaViewer/base.ts`
(:81-105 константы, :195-316 поля, :318-445 конструктор DOM,
:1847-1856 toggleWholeActive, :1958-1980 setNewMover, :2452-2455 монтирование),
`src/components/mediaViewer/mediaViewer.scss`, `src/components/button.ts` /
`buttonIcon.ts` / `icon.ts`, `src/helpers/appWindow.ts:33`.

## 1. `web-client/src/components/mediaViewer/base.ts`

Vanilla-класс `AppMediaViewerBase<ContentAdditionType, ButtonsAdditionType,
TargetType>` extends `EventListenerBase<{setMoverBefore, setMoverAfter}>` —
ИНЕРТЕН: никто его не создаёт, точки входа/открытие — Task 11+.

Дерево конструктора 1:1 tweb (порядок append load-bearing — CSS-соседи
`.zoom-container.is-visible ~ .media-viewer-caption` / `~ .media-viewer-movers`,
mediaViewer.scss:725,730):

```
div.media-viewer-whole
  div.zoom-container                 (только !IS_TOUCH_SUPPORTED, tweb :398-400)
    button.btn-icon (zoomout) · div.progress-line.with-transition · button.btn-icon (zoomin)
  div.overlays > div.media-viewer > .media-viewer-content
                 > .media-viewer-container > .media-viewer-media (layout-ghost)
  div.media-viewer-topbar.media-viewer-appear
    .media-viewer-topbar-left: btn-icon.only-handhelds (mobile-close)
                               + .media-viewer-author.no-select > -author-right > -name + -date
    .media-viewer-buttons: topButtons ++ [download, rotate(глиф rotate_left), zoomin, close]
  div.media-viewer-movers
    .media-viewer-switcher-left/right > span.tgico.media-viewer-sibling-button
    .media-viewer-mover-wrapper > .media-viewer-mover   (стартовый setNewMover)
```

Константы tweb :81-105 целиком: `OPEN/MOVE_TRANSITION_TIME = 200/350`,
`ZOOM_STEP/INITIAL/MIN/MAX = .5/1/.5/4`, `RESERVE_TOP/BOTTOM_DESKTOP = 80/110`,
`RESERVE_*_MOBILE = 0/0`, `VIDEO_MIN_WIDTH = 420`,
`USE_MEDIA_VIEWER_CLIP_PATH = true`, `NO_MEDIA_VIEWER_CLIP_PATH = 'inset(0px)'`,
`MEDIA_VIEWER_CLASSNAME = 'media-viewer'`.

Методы каркаса: `getOverlayRoot()` (= `document.body`), `mountToOverlay()`
(append + reflow, tweb :2452-2455), `toggleWholeActive` 1:1 (:1847-1856 —
`backwards` + setTimeout 0), `setNewMover()` 1:1 (:1958-1980 — wrapper > mover
со стартовым `visibility: hidden; width: 1px; height: 1px;`, свой
middlewareHelper), `destroy()`. Заготовки полей: `listLoader` (Task 3),
`preloader`/`preloaderStreamable` (Task 4, сконструированы), `swipeHandler`
(null до Task 12), карты `buttons`/`content {main,container,media,mover}`,
`zoomElements`, `middlewareHelper`, `overlayActive`, `setMover*Promise`.

Адаптации (все прокомментированы в файле):
- `ButtonIcon`/`Icon` tweb → локальные `btnIcon`/`iconSpan` — та же разметка
  `button.btn-icon > span.tgico.button-icon`, глифы из `@core/tgico-icons`;
  ripple у mobile-close не портирован (наш ripple — React-хук), приедет с
  оживлением кнопок (Task 13);
- RangeSelector зума — пока голый `div.progress-line.with-transition`
  (те же классы, что строит tweb RangeSelector c `withTransition: true`),
  наполнение — Task 12;
- `getOverlayRoot()`: у tweb body активного окна (Document-PiP всего
  приложения), у нас — body главного документа;
- `middlewareHelper` мувера: у tweb на HTMLElement глобально (global.d.ts:27),
  у нас — локальный тип `MoverElement` + `Object.assign` (без каста);
- `lazyLoadQueue` не портирован (нужен только листанию — Task 14); caption
  создаёт подкласс (Task 14); аватарка автора — React-остров (Task 13).

## 2. Сверка партиала `styles/tweb/_mediaViewer.scss`

`diff` против tweb `src/components/mediaViewer/mediaViewer.scss`:
**0 расхождений по правилам, до и после** (правки не потребовались) —
файл байт-в-байт, единственная строка отличия — `@use "../foundation"` вместо
`@use "../../scss/shared"` (адаптация пути импорта миксинов).

Проверено наличие: `.chrome-hidden` (:588) и `.has-video:not(.has-video-controls)`
(:585), `.highlight-switchers` (:610), `-mover-wrapper` c `will-change: clip-path`
+ `transition: clip-path var(--open-duration)` (:639-648), `z-index: 4` у
`.media-viewer-whole` (:533) и `.overlays` (:745). Партиал подключён в
`styles/tweb/_index.scss:51`.

## 3. Тесты (TDD, happy-dom) — `base.test.ts`, 12 тестов

RED наблюдался (модуль `./base` отсутствовал), затем GREEN. Пины:
- порядок детей wholeDiv ровно `[zoom-container, overlays, topbar, movers]`
  (+ `media-viewer-appear` на топбаре);
- подветки: overlays-цепочка до layout-ghost `-media`; topbar-left
  (mobile-close + author right/name/date); свитчеры со
  `span.tgico.media-viewer-sibling-button`; zoom-container `[−, полоса, +]`;
- кнопки в порядке `topButtons + [download, rotate, zoomin, close]`, rotate —
  глиф `rotate_left` под ключом `rotate`;
- `setNewMover`: `wrapper > mover` со стартовым cssText, новый wrapper — рядом
  со старым в moversContainer;
- `toggleWholeActive(false)` (fake timers): `backwards` синхронно, `active`
  снимается только по таймеру 0;
- `mountToOverlay` — body, идемпотентно; `destroy` снимает wholeDiv.

Мутации (обе краснят, реальный вывод vitest):

1. Порядок append (`append(topbar, overlaysDiv, …)` вместо
   `append(overlaysDiv, topbar, …)`):
   ```
   FAIL … порядок детей wholeDiv ровно [zoom-container, overlays, topbar, movers]
   - Expected  + Received
     [ "zoom-container", - "overlays", "media-viewer-topbar", + "overlays", "media-viewer-movers" ]
   ```
2. Backwards-механика (снятие `active` синхронно вместо setTimeout 0):
   ```
   FAIL … false — ставит backwards, а active снимает только по таймеру 0
   AssertionError: expected false to be true
   185| expect(v.whole.classList.contains('active')).toBe(true)
   ```

## 4. Проверки

- `npx vitest run`: **224 файла / 1533 passed, 2 skipped** — зелёный;
- `npm run typecheck` (tsc --noEmit, TS7 native): чисто;
- oxlint: суммарно 2593 диагностики с новыми файлами и без них (дельта 0);
  точечный `npx oxlint --type-aware base.ts base.test.ts` — 0 диагностик;
- `npx vite build --outDir /tmp/media-task10-build`: успешно (1.7s).
